# E09 — Drift Gradual por Fração de Mistura — Plano de Implementação

> **Para agentic workers:** REQUIRED SUB-SKILL: Use subagent-driven-development (recommended) ou executing-plans para implementar este plano tarefa por tarefa. Passos usam checkbox (`- [ ]`).
>
> Documento de plano, revisão 19 — 2026-08-09. Especificação: doc 18 de
> `docs/literatura/` e `checkpoints/e09-drift-gradual/`.

**Goal:** Adicionar drift gradual ao fluxo do E07: a fração de mistura dos
dados corrompidos sobe de 0 a 1 de forma linear, com a avaliação e o
retreino acompanhando a fração corrente.

**Architecture:** Um campo novo `drift_ramp_ticks` no `DriftEpisodeConfig`.
`None` reproduz o E07 por inteiro. A fração é função do tempo virtual
desde a produção: `f(v) = min(1, (v - production_start) / (N x 10))`. O
batch de monitoramento corrompe `round(f x 32)` imagens escolhidas pelo RNG
do episódio. O teste de avaliação mistura imagens corrompidas e limpas na
fração corrente, com permutação determinística. O retreino usa a mistura na
fração do momento do gatilho. Um grid novo em `experiments/e09_drift_gradual/`
segue o padrão do grid do E08.

**Tech Stack:** Python, NumPy, PyTorch, unittest (sem pytest). Sem
dependências novas.

## Global Constraints

- Testes com unittest: `.venv/bin/python -m unittest tests.<modulo> -v`.
- Não usar pytest nem adicionar dependências.
- Caminho `drift_ramp_ticks=None` deve reproduzir o E07 byte a byte: sem
  consumo extra de RNG no batch, sem teste misto.
- A fração é função do tempo virtual, não do contador de ticks. Ela
  continua subindo durante o retreino.
- O subconjunto corrompido nunca usa prefixo fixo (CIFAR-10 ordenado por
  classe). Usar permutação/RNG determinístico.
- `drift_ramp_ticks` junto com `drift_onset_ticks` ou
  `drifted_client_ids` é erro de configuração, rejeitado na validação.
- Commits no estilo do repo: `feat(e09): ...`, `test(e09): ...`.
- A suíte atual roda 167 testes com 5 falhas pré-existentes do backend MPS
  (documentadas no STATE.md do E07). Não introduzir falhas novas.
- Rodar `python -m compileall -q experiments tests` e `git diff --check`
  ao final de cada tarefa.

---

### Task 1: Campo de configuração e helpers de fração

**Files:**
- Modify: `experiments/e07_drift_agent/episode.py` (dataclass `DriftEpisodeConfig` + validação em `run_drift_episode`)
- Modify: `experiments/e07_drift_agent/drift_schedule.py`
- Test: `tests/test_drift_schedule.py`, `tests/test_drift_episode.py`

**Interfaces:**
- Consumes: `DriftEpisodeConfig` existente (dataclass frozen).
- Produces:
  - `drift_schedule.ramp_fraction(virtual_seconds_since_production: float, config: DriftEpisodeConfig) -> float`
  - `drift_schedule.corrupt_count(size: int, fraction: float) -> int`
  - Config validado em `run_drift_episode` (tipo e combinação).

- [ ] **Step 1: Escrever os testes que falham**

Adicionar a `tests/test_drift_schedule.py`:

```python
from experiments.e07_drift_agent.drift_schedule import corrupt_count, ramp_fraction
from experiments.e07_drift_agent.episode import DriftEpisodeConfig


class TestRampFraction(unittest.TestCase):
    def setUp(self):
        self.config = DriftEpisodeConfig(
            drift_ramp_ticks=20, monitor_tick_seconds=10.0
        )

    def test_fraction_is_zero_at_production_start(self):
        self.assertEqual(ramp_fraction(0.0, self.config), 0.0)

    def test_fraction_is_half_after_ten_ticks(self):
        self.assertEqual(ramp_fraction(100.0, self.config), 0.5)

    def test_fraction_clamps_at_one(self):
        self.assertEqual(ramp_fraction(300.0, self.config), 1.0)

    def test_fraction_is_one_without_ramp(self):
        config = DriftEpisodeConfig()
        self.assertEqual(ramp_fraction(0.0, config), 1.0)

    def test_corrupt_count_rounds_the_fraction(self):
        self.assertEqual(corrupt_count(32, 0.0), 0)
        self.assertEqual(corrupt_count(32, 0.5), 16)
        self.assertEqual(corrupt_count(32, 1.0), 32)
        self.assertEqual(corrupt_count(4, 0.75), 3)
```

Adicionar a `tests/test_drift_episode.py`:

```python
    def test_drift_ramp_ticks_rejects_invalid_values_and_combinations(self):
        cases = (
            dict(drift_ramp_ticks=0),
            dict(drift_ramp_ticks=True),
            dict(drift_ramp_ticks=-3),
        )
        for overrides in cases:
            with self.subTest(overrides=overrides):
                with self.assertRaisesRegex(ValueError, "drift_ramp_ticks"):
                    run_drift_episode(
                        _config(**overrides),
                        server=_FakeServer(),
                        monitor=_ScriptedMonitor([]),
                        corruption_fn=_identity,
                    )

    def test_drift_ramp_ticks_rejects_combination_with_onset_fields(self):
        with self.assertRaisesRegex(ValueError, "drift_ramp_ticks"):
            run_drift_episode(
                _config(
                    drift_ramp_ticks=20,
                    drifted_client_ids=(0, 1),
                ),
                server=_FakeServer(),
                monitor=_ScriptedMonitor([]),
                corruption_fn=_identity,
            )
        with self.assertRaisesRegex(ValueError, "drift_ramp_ticks"):
            run_drift_episode(
                _config(drift_ramp_ticks=20, drift_onset_ticks={0: 1}),
                server=_FakeServer(),
                monitor=_ScriptedMonitor([]),
                corruption_fn=_identity,
            )
```

- [ ] **Step 2: Rodar e confirmar que falham**

Run: `.venv/bin/python -m unittest tests.test_drift_schedule tests.test_drift_episode -v`
Expected: FAIL com `ImportError`/`AttributeError` de `ramp_fraction`/`corrupt_count` e `ValueError` não levantado.

- [ ] **Step 3: Implementar os helpers**

Adicionar a `experiments/e07_drift_agent/drift_schedule.py`:

```python
def ramp_fraction(
    virtual_seconds_since_production: float,
    config: DriftEpisodeConfig,
) -> float:
    """Return the mixture fraction at a production-relative virtual time.

    Without a configured ramp, the fraction is always 1, which reproduces
    the E07 all-corrupted regime.  With a ramp, the fraction grows linearly
    from 0 to 1 over ``drift_ramp_ticks`` monitor ticks.
    """
    if config.drift_ramp_ticks is None:
        return 1.0
    duration = config.drift_ramp_ticks * config.monitor_tick_seconds
    return min(1.0, max(0.0, virtual_seconds_since_production / duration))


def corrupt_count(size: int, fraction: float) -> int:
    """Return how many of ``size`` items are corrupted at a fraction."""
    return min(size, round(fraction * size))
```

Adicionar a validação no início de `run_drift_episode` em `episode.py`
(depois das validações existentes de `retrain_rounds`/`warmup_ticks`):

```python
    if config.drift_ramp_ticks is not None and (
        isinstance(config.drift_ramp_ticks, bool) or config.drift_ramp_ticks < 1
    ):
        raise ValueError("drift_ramp_ticks must be None or an integer >= 1")
    if config.drift_ramp_ticks is not None and (
        config.drift_onset_ticks is not None
        or config.drifted_client_ids is not None
    ):
        raise ValueError(
            "drift_ramp_ticks cannot be combined with drift_onset_ticks "
            "or drifted_client_ids"
        )
```

Adicionar o campo ao dataclass `DriftEpisodeConfig` (após
`drift_onset_ticks`):

```python
    drift_ramp_ticks: int | None = None
```

- [ ] **Step 4: Rodar e confirmar que passam**

Run: `.venv/bin/python -m unittest tests.test_drift_schedule tests.test_drift_episode -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add experiments/e07_drift_agent/episode.py experiments/e07_drift_agent/drift_schedule.py tests/test_drift_schedule.py tests/test_drift_episode.py
git commit -m "feat(e09): add drift ramp fraction helpers and config field"
```

---

### Task 2: Rampa no batch de monitoramento

**Files:**
- Modify: `experiments/e07_drift_agent/episode.py` (`_monitor_batches` + call site no loop de produção)
- Test: `tests/test_drift_episode.py`

**Interfaces:**
- Consumes: `ramp_fraction`, `corrupt_count` (Task 1).
- Produces: `_monitor_batches(client_datasets, config, corruption_fn, rng, *, tick, production_seconds=None)` — novo parâmetro `production_seconds` (segundos virtuais desde o início da produção; o warm-up tem duração virtual zero). Com rampa, corrompe `corrupt_count(len(batch), f)` imagens via `rng.choice(len(batch), ...)`. Sem rampa, corrompe o batch inteiro (byte a byte igual ao E07).

- [ ] **Step 1: Escrever os testes que falham**

Adicionar a `tests/test_drift_episode.py`:

```python
    def test_ramp_corrupts_a_growing_fraction_of_monitor_batches(self):
        from experiments.e07_drift_agent.episode import _monitor_batches

        def tagging_corruption(batch, _kind, _severity, seed=None):
            return batch + 1.0

        rng = np.random.default_rng(42)
        x_clean = np.zeros((64, 1, 1, 1), dtype=np.float32)
        y_clean = np.zeros(64, dtype=np.int64)
        datasets = [(x_clean, y_clean) for _ in range(2)]
        config = _config(
            num_clients=2,
            drift_ramp_ticks=20,
            batch_size=32,
            seed=42,
            warmup_ticks=2,
        )
        first = _monitor_batches(
            datasets, config, tagging_corruption, rng,
            tick=20, production_seconds=0.0,
        )
        self.assertEqual(float(first["0"].sum()), 0.0)  # fração 0
        self.assertEqual(float(first["1"].sum()), 0.0)
        mid = _monitor_batches(
            datasets, config, tagging_corruption, rng,
            tick=30, production_seconds=100.0,
        )
        self.assertEqual(float(mid["0"].sum()), 16.0)  # fração 0.5 -> 16 de 32
        self.assertEqual(float(mid["1"].sum()), 16.0)
        end = _monitor_batches(
            datasets, config, tagging_corruption, rng,
            tick=40, production_seconds=300.0,
        )
        self.assertEqual(float(end["0"].sum()), 32.0)  # fração 1
        self.assertEqual(float(end["1"].sum()), 32.0)

    def test_ramp_batch_is_deterministic_across_arms(self):
        from experiments.e07_drift_agent.episode import _monitor_batches

        def tagging_corruption(batch, _kind, _severity, seed=None):
            return batch + 1.0

        x_clean = np.zeros((64, 32, 32, 3), dtype=np.float32)
        y_clean = np.zeros(64, dtype=np.int64)
        datasets = [(x_clean, y_clean) for _ in range(2)]
        config = _config(num_clients=2, drift_ramp_ticks=20, batch_size=32, seed=7, warmup_ticks=2)

        first = _monitor_batches(
            datasets, config, tagging_corruption, np.random.default_rng(7),
            tick=30, production_seconds=100.0,
        )
        second = _monitor_batches(
            datasets, config, tagging_corruption, np.random.default_rng(7),
            tick=30, production_seconds=100.0,
        )
        self.assertTrue(torch.equal(first["0"], second["0"]))
        self.assertTrue(torch.equal(first["1"], second["1"]))
```

- [ ] **Step 2: Rodar e confirmar que falham**

Run: `.venv/bin/python -m unittest tests.test_drift_episode -v`
Expected: FAIL (batches com soma 0, pois a rampa ainda não corrompe nada).

- [ ] **Step 3: Implementar a rampa no `_monitor_batches`**

Substituir `_monitor_batches` em `episode.py`:

```python
def _monitor_batches(
    client_datasets: list[tuple[np.ndarray, np.ndarray]],
    config: DriftEpisodeConfig,
    corruption_fn: CorruptionFn | None,
    rng: np.random.Generator,
    *,
    tick: int,
    production_seconds: float | None = None,
) -> dict[str, torch.Tensor]:
    """Sample per-client monitor batches, corrupting drifted clients.

    ``tick`` is the absolute monitor tick (warm-up included).  The drift
    schedule is evaluated in the production-relative frame so that onset 0
    lands on the first corrupted production tick; the corruption seed stays
    on the absolute tick to keep the E07 sequence byte-identical.  With a
    configured ramp, the batch corrupts only ``round(fraction x batch)``
    images chosen through ``rng``, where the fraction follows
    ``production_seconds``, the virtual time elapsed since production
    start (the warm-up phase has zero virtual duration).
    """
    batches: dict[str, torch.Tensor] = {}
    schedule_tick = tick - config.warmup_ticks
    fraction = None
    if config.drift_ramp_ticks is not None:
        production_seconds_value = (
            float(production_seconds)
            if production_seconds is not None
            else schedule_tick * config.monitor_tick_seconds
        )
        fraction = ramp_fraction(production_seconds_value, config)
    for position, (x, _y) in enumerate(client_datasets):
        batch = _sample_batch(x, config.batch_size, rng)
        if corruption_fn is not None and is_client_drifted_at_tick(
            position, schedule_tick, config
        ):
            corruption_seed = config.seed + 10_000 * tick + position
            if fraction is None:
                batch = _call_corruption(
                    corruption_fn, batch, config, seed=corruption_seed
                )
            else:
                count = corrupt_count(len(batch), fraction)
                if count:
                    indices = rng.choice(len(batch), size=count, replace=False)
                    corrupted = _call_corruption(
                        corruption_fn, batch, config, seed=corruption_seed
                    )
                    batch[indices] = corrupted[indices]
        # The monitor contract is deliberately x-only.
        batches[str(position)] = torch.from_numpy(batch)
    return batches
```

Adicionar o import no topo de `episode.py` (junto do import de `drift_schedule`):

```python
from experiments.e07_drift_agent.drift_schedule import (
    build_retrain_datasets,
    corrupt_count,
    is_client_drifted_at_tick,
    ramp_fraction,
)
```

Atualizar o call site no loop de produção de `run_drift_episode`:

```python
        server.virtual_time += advance
        outcome = monitor.observe_tick(
            _monitor_batches(
                clean_client_datasets, config, corruption_fn, rng,
                tick=config.warmup_ticks + tick,
                production_seconds=float(server.virtual_time) - production_start,
            ),
            virtual_time=float(server.virtual_time),
            record_action=not config.baseline,
        )
```

- [ ] **Step 4: Rodar e confirmar que passam**

Run: `.venv/bin/python -m unittest tests.test_drift_episode tests.test_drift_e2e_smoke -v`
Expected: PASS (inclui os testes antigos do E08 de onset por cliente, que usam `tick` sem `production_seconds`).

- [ ] **Step 5: Commit**

```bash
git add experiments/e07_drift_agent/episode.py tests/test_drift_episode.py
git commit -m "feat(e09): corrupt a growing batch fraction under the drift ramp"
```

---

### Task 3: Teste misto na avaliação e métricas da fração

**Files:**
- Modify: `experiments/e07_drift_agent/episode.py` (`_mixed_evaluation_test`, `_current_evaluation_test`, call sites de avaliação, `_finish_result`, `_run_retraining`)
- Test: `tests/test_drift_episode.py`

**Interfaces:**
- Consumes: `ramp_fraction`, `corrupt_count`.
- Produces:
  - `_mixed_evaluation_test(clean_test, corrupted_test, permutation, fraction) -> tuple[np.ndarray, np.ndarray]`
  - `_current_evaluation_test(server, config, production_start, clean_test, corrupted_test, permutation) -> tuple[np.ndarray, np.ndarray]`
  - Métricas novas quando a rampa está ativa: `fraction_at_trigger`, `fraction_at_tau_crossing`.

- [ ] **Step 1: Escrever os testes que falham**

Adicionar a `tests/test_drift_episode.py`:

```python
    def test_ramp_records_fraction_metrics_and_uses_mixed_test(self):
        config = _config(drift_ramp_ticks=20, production_horizon_seconds=400.0)
        server = _FakeServer(
            round_durations=(2.0, 3.0, 5.0, 7.0, 11.0, 13.0, 2.0),
            evaluation_accuracies=[
                0.9,  # training round (clean)
                0.9,  # pre-drift clean evaluation
                0.9,  # drift onset (teste misto na fração 0 -> limpo)
                0.6, 0.6, 0.6,  # monitor ticks antes da decisão
                0.2, 0.2, 0.2, 0.2, 0.4,  # cinco rounds de retreino
                0.4, 0.4, 0.4, 0.4,  # ticks pós-retreino
                0.4, 0.9,  # episode end e final clean
            ],
        )
        result = run_drift_episode(
            config,
            server=server,
            monitor=_ScriptedMonitor([False, False, True]),
            corruption_fn=_identity,
        )

        production_start = result["metadata"]["production_start_time"]
        decision_time = result["retrain_decisions"][0]["time"]
        self.assertAlmostEqual(
            result["metrics"]["fraction_at_trigger"],
            (decision_time - production_start) / 200.0,
        )
        crossing = next(
            entry for entry in result["corrupted_accuracy_history"]
            if entry["accuracy"] < 0.5
        )
        self.assertAlmostEqual(
            result["metrics"]["fraction_at_tau_crossing"],
            (crossing["time"] - production_start) / 200.0,
        )
        onset = next(
            entry for entry in result["corrupted_accuracy_history"]
            if entry["stage"] == "drift_onset"
        )
        self.assertEqual(onset["accuracy"], 0.9)

    def test_ramp_metrics_are_absent_without_ramp(self):
        result = run_drift_episode(
            _config(production_horizon_seconds=400.0),
            server=_FakeServer(
                round_durations=(2.0, 3.0, 5.0, 7.0, 11.0, 13.0, 2.0)
            ),
            monitor=_ScriptedMonitor([False, False, True]),
            corruption_fn=_identity,
        )
        self.assertNotIn("fraction_at_trigger", result["metrics"])
        self.assertNotIn("fraction_at_tau_crossing", result["metrics"])
```

Aritmética esperada: o treino inicial tem 1 round de 2.0 s. A produção
começa em t=2.0. A decisão ocorre no 3º tick, em t=32.0. A fração no
gatilho vale (32.0 - 2.0) / (20 x 10) = 0.15. O cruzamento de tau ocorre na
primeira avaliação com acurácia abaixo de 0.5, no primeiro round de
retreino, em t=35.0. A fração vale 0.165. Os asserts genéricos acima
calculam os valores a partir do resultado, sem hardcode.

- [ ] **Step 2: Rodar e confirmar que falham**

Run: `.venv/bin/python -m unittest tests.test_drift_episode -v`
Expected: FAIL (`KeyError: 'fraction_at_trigger'`).

- [ ] **Step 3: Implementar o teste misto e as métricas**

Atenção: o teste existente `test_retraining_uses_clean_data_for_clients_not_yet_drifted`
em `tests/test_drift_episode.py` chama `_run_retraining` com a assinatura
antiga. Atualizar a chamada nesse teste para a nova assinatura:

```python
        result = {"retrain_round_events": [], "corrupted_accuracy_history": []}
        config = _config(num_clients=2, drifted_client_ids=(0,), drift_onset_ticks={0: 1})
        _run_retraining(
            result, server, [corrupted, corrupted], 1,
            config, production_start_time=0.0,
            current_test_fn=lambda: corrupted_test,
        )
```

Adicionar a `episode.py` (junto de `_record_evaluation`):

```python
MIXTURE_PERMUTATION_SEED_OFFSET = 12_345


def _mixed_evaluation_test(
    clean_test: tuple[np.ndarray, np.ndarray],
    corrupted_test: tuple[np.ndarray, np.ndarray],
    permutation: np.ndarray | None,
    fraction: float,
) -> tuple[np.ndarray, np.ndarray]:
    """Mix the clean and corrupted tests at a fraction.

    Without a permutation, the fully corrupted test is returned, which
    reproduces the E07 evaluation.
    """
    if permutation is None:
        return corrupted_test
    count = corrupt_count(len(clean_test[0]), fraction)
    x = clean_test[0].copy()
    x[permutation[:count]] = corrupted_test[0][permutation[:count]]
    return x, clean_test[1]


def _current_evaluation_test(
    server,
    config: DriftEpisodeConfig,
    production_start: float,
    clean_test: tuple[np.ndarray, np.ndarray],
    corrupted_test: tuple[np.ndarray, np.ndarray],
    permutation: np.ndarray | None,
) -> tuple[np.ndarray, np.ndarray]:
    """Return the evaluation test for the current production fraction."""
    if config.drift_ramp_ticks is None:
        return corrupted_test
    fraction = ramp_fraction(
        float(server.virtual_time) - production_start, config
    )
    return _mixed_evaluation_test(
        clean_test, corrupted_test, permutation, fraction
    )
```

Em `run_drift_episode`, após criar `corrupted_test`:

```python
    mixture_permutation = None
    client_permutations = None
    if config.drift_ramp_ticks is not None:
        permutation_rng = np.random.default_rng(
            config.seed + MIXTURE_PERMUTATION_SEED_OFFSET
        )
        mixture_permutation = permutation_rng.permutation(len(clean_test[0]))
        client_permutations = [
            permutation_rng.permutation(len(x)) for x, _y in clean_client_datasets
        ]
    result["_mixture_permutation"] = mixture_permutation
    result["_client_permutations"] = client_permutations
```

Substituir as avaliações corrompidas da produção. Onde hoje é
`_record_evaluation(result["corrupted_accuracy_history"], server, corrupted_test, "drift_onset")`, usar:

```python
    _record_evaluation(
        result["corrupted_accuracy_history"],
        server,
        _current_evaluation_test(
            server, config, production_start, clean_test, corrupted_test,
            mixture_permutation,
        ),
        "drift_onset",
    )
```

No loop de produção, onde hoje é
`_record_evaluation(result["corrupted_accuracy_history"], server, corrupted_test, "monitor_tick")`, usar o mesmo `_current_evaluation_test(...)`.

Em `_run_retraining`, substituir o parâmetro `corrupted_test` por um callable `current_test_fn`:

```python
def _run_retraining(
    result: dict,
    server,
    corrupted_client_datasets: list[tuple[np.ndarray, np.ndarray]],
    rounds: int,
    config: DriftEpisodeConfig,
    production_start_time: float,
    *,
    current_test_fn: Callable[[], tuple[np.ndarray, np.ndarray]],
    client_permutations: list[np.ndarray] | None = None,
) -> None:
    clean_client_datasets = [
        (np.asarray(client.dataset[0]).copy(), np.asarray(client.dataset[1]).copy())
        for client in server.clients
    ]
    if config.drift_ramp_ticks is None:
        current_tick = int(
            (float(server.virtual_time) - production_start_time)
            // config.monitor_tick_seconds
        )
        retrain_datasets = build_retrain_datasets(
            clean_client_datasets,
            corrupted_client_datasets,
            current_tick,
            config=config,
        )
    else:
        fraction = ramp_fraction(
            float(server.virtual_time) - production_start_time, config
        )
        permutations = client_permutations or [
            np.arange(len(x)) for x, _y in clean_client_datasets
        ]
        retrain_datasets = [
            build_mixed_dataset(clean, corrupted, fraction, permutation)
            for (clean, corrupted), permutation in zip(
                zip(clean_client_datasets, corrupted_client_datasets), permutations
            )
        ]
    for client, dataset in zip(server.clients, retrain_datasets):
        client.dataset = dataset
        if hasattr(client, "reset_optimizer"):
            client.reset_optimizer()
    for _ in range(rounds):
        event = server.run_one_round(record_default_metrics=False)
        result["retrain_round_events"].append(event)
        _record_evaluation(
            result["corrupted_accuracy_history"], server, current_test_fn(),
            "retrain_round",
        )
```

O call site de `_run_retraining` em `run_drift_episode`:

```python
            _run_retraining(
                result,
                server,
                corrupted_client_datasets,
                config.retrain_rounds,
                config,
                production_start,
                current_test_fn=lambda: _current_evaluation_test(
                    server, config, production_start, clean_test,
                    corrupted_test, mixture_permutation,
                ),
                client_permutations=client_permutations,
            )
```

Em `_finish_result`, antes do `del result["_monitor"]`, adicionar as métricas:

```python
    if config.drift_ramp_ticks is not None:
        if result["retrain_decisions"]:
            decision_time = float(result["retrain_decisions"][0]["time"])
            result["metrics"]["fraction_at_trigger"] = ramp_fraction(
                decision_time - float(result["metadata"]["production_start_time"]),
                config,
            )
        crossing = next(
            (
                float(entry["time"])
                for entry in result["corrupted_accuracy_history"]
                if float(entry["accuracy"]) < config.tau
            ),
            None,
        )
        if crossing is not None:
            result["metrics"]["fraction_at_tau_crossing"] = ramp_fraction(
                crossing - float(result["metadata"]["production_start_time"]),
                config,
            )
```

E remover as chaves temporárias no fim de `_finish_result`:

```python
    del result["_monitor"]
    del result["_corrupted_test"]
    del result["_mixture_permutation"]
    del result["_client_permutations"]
```

Adicionar `build_mixed_dataset` a `drift_schedule.py` (consumido por esta tarefa):

```python
def build_mixed_dataset(
    clean: tuple[Any, Any],
    corrupted: tuple[Any, Any],
    fraction: float,
    permutation: np.ndarray,
) -> tuple[Any, Any]:
    """Mix a dataset at a fraction using a deterministic permutation."""
    x = np.asarray(clean[0]).copy()
    y = np.asarray(clean[1]).copy()
    count = corrupt_count(len(x), fraction)
    x[permutation[:count]] = np.asarray(corrupted[0])[permutation[:count]]
    return x, y
```

- [ ] **Step 4: Rodar e confirmar que passam**

Run: `.venv/bin/python -m unittest tests.test_drift_episode tests.test_drift_schedule tests.test_drift_e2e_smoke -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add experiments/e07_drift_agent/episode.py experiments/e07_drift_agent/drift_schedule.py tests/test_drift_episode.py tests/test_drift_schedule.py
git commit -m "feat(e09): evaluate and retrain on the current mixture fraction"
```

---

### Task 4: Schema v3, experiment_config, resumo e diretórios

**Files:**
- Modify: `experiments/e07_drift_agent/episode.py` (`_experiment_config`)
- Modify: `experiments/shared/drift_results.py` (`_validate_v3_internal_consistency`)
- Modify: `experiments/e07_drift_agent/run_matrix.py` (`_summary_dimensions`, `_result_directory`)
- Test: `tests/test_drift_results.py`, `tests/test_drift_episode.py`, `tests/test_drift_runner.py`

**Interfaces:**
- Consumes: campo `drift_ramp_ticks` (Task 1).
- Produces: campo serializado no `experiment_config`, validado na v3, com chave própria no resumo e desambiguação de diretório.

- [ ] **Step 1: Escrever os testes que falham**

Adicionar a `tests/test_drift_results.py`:

```python
    def test_v3_accepts_ramp_ticks_and_rejects_bad_values(self):
        payload = _v3_payload()
        payload["experiment_config"]["drift_ramp_ticks"] = 20
        DriftResult.from_payload(payload)

        for bad in (0, -1, True, 2.5):
            with self.subTest(value=bad):
                payload = _v3_payload()
                payload["experiment_config"]["drift_ramp_ticks"] = bad
                with self.assertRaisesRegex(ValueError, "drift_ramp_ticks"):
                    DriftResult.from_payload(payload)

    def test_v3_rejects_ramp_ticks_combined_with_onset_fields(self):
        for field, value in (
            ("drift_onset_ticks", {0: 1}),
            ("drifted_client_ids", [0, 1]),
        ):
            with self.subTest(field=field):
                payload = _v3_payload()
                payload["experiment_config"]["drift_ramp_ticks"] = 20
                payload["experiment_config"][field] = value
                with self.assertRaisesRegex(ValueError, "drift_ramp_ticks"):
                    DriftResult.from_payload(payload)
```

Adicionar a `tests/test_drift_runner.py` (ver o arquivo antes; usar o padrão de
teste de resumo existente do arquivo):

```python
    def test_summary_and_directories_partition_by_ramp_duration(self):
        from experiments.e07_drift_agent.run_matrix import (
            _result_directory,
            _summarize_matrix_runs,
            _summary_dimensions,
        )

        base = DriftEpisodeConfig(corruption="fog", severity=4, seed=42)
        configs = [
            replace(base, drift_ramp_ticks=5),
            replace(base, drift_ramp_ticks=20),
        ]
        duplicates = Counter((c.corruption, c.severity, c.seed) for c in configs)
        directories = {
            _result_directory(Path("/tmp/e09"), config, duplicates)
            for config in configs
        }
        self.assertEqual(len(directories), 2)
        keys = {
            _summary_group_key(_summary_dimensions(config, population="visual", trigger_policy="detector"))
            for config in configs
        }
        self.assertEqual(len(keys), 2)
```

- [ ] **Step 2: Rodar e confirmar que falham**

Run: `.venv/bin/python -m unittest tests.test_drift_results tests.test_drift_runner -v`
Expected: FAIL (`ValueError` não levantado; diretórios iguais).

- [ ] **Step 3: Implementar**

Em `episode.py`, `_experiment_config`, após `"drift_onset_ticks"`:

```python
        "drift_ramp_ticks": config.drift_ramp_ticks,
```

Em `drift_results.py`, `_validate_v3_internal_consistency`, após a validação
de `retrain_rounds`:

```python
    ramp = config.get("drift_ramp_ticks")
    if ramp is not None and (
        not isinstance(ramp, int) or isinstance(ramp, bool) or ramp < 1
    ):
        raise ValueError(
            "schema v3 drift_ramp_ticks must be None or an integer >= 1"
        )
    if ramp is not None and (
        config.get("drift_onset_ticks") is not None
        or config.get("drifted_client_ids") is not None
    ):
        raise ValueError(
            "schema v3 drift_ramp_ticks cannot be combined with "
            "drift_onset_ticks or drifted_client_ids"
        )
```

Em `run_matrix.py`, `_summary_dimensions`, após `drift_onset_ticks`:

```python
        "drift_ramp_ticks": config.drift_ramp_ticks,
```

Em `run_matrix.py`, `_result_directory`, no bloco de desambiguadores, após o
bloco do `drift_onset_ticks`:

```python
        if config.drift_ramp_ticks is not None:
            disambiguators += f"_ramp_{config.drift_ramp_ticks}"
```

Atualizar o teste `test_schema_v3_persists_auditable_experiment_detector_and_runtime_metadata`
em `tests/test_drift_episode.py`: o dict esperado de `experiment_config` ganha
`"drift_ramp_ticks": None` (após `"drift_onset_ticks": None`).

- [ ] **Step 4: Rodar e confirmar que passam**

Run: `.venv/bin/python -m unittest tests.test_drift_results tests.test_drift_runner tests.test_drift_episode -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add experiments/e07_drift_agent/episode.py experiments/shared/drift_results.py experiments/e07_drift_agent/run_matrix.py tests/test_drift_results.py tests/test_drift_runner.py tests/test_drift_episode.py
git commit -m "feat(e09): persist, validate, and summarize the ramp configuration"
```

---

### Task 5: Registro da família e grid do E09

**Files:**
- Create: `experiments/e09_drift_gradual/__init__.py`, `experiments/e09_drift_gradual/run_grid.py`
- Modify: `experiments/shared/registry.py`
- Test: `tests/test_e09_grid.py`, `tests/test_experiment_registry.py`

**Interfaces:**
- Consumes: `run_matrix` do E07, `DriftEpisodeConfig`, `temporary_output_path`.
- Produces: `e09_drift_gradual.build_grid(*, seeds, scenarios, ramp_ticks) -> list[DriftEpisodeConfig]` e CLI `python -m experiments.e09_drift_gradual.run_grid`.

- [ ] **Step 1: Escrever os testes que falham**

Criar `tests/test_e09_grid.py`:

```python
"""Grid builder and smoke runner for the E09 gradual-drift experiment."""

import unittest
from collections import Counter
from pathlib import Path

from experiments.e09_drift_gradual.run_grid import DEFAULT_SCENARIOS, build_grid


class TestBuildGrid(unittest.TestCase):
    def test_build_grid_covers_scenarios_seeds_and_ramp_durations(self):
        configs = build_grid()
        self.assertEqual(len(configs), 4 * 5 * 3)
        self.assertEqual(
            {c.drift_ramp_ticks for c in configs}, {5, 10, 20}
        )
        self.assertEqual({c.seed for c in configs}, {42, 43, 44, 45, 46})
        self.assertEqual(
            {(c.corruption, c.severity) for c in configs},
            set(DEFAULT_SCENARIOS),
        )

    def test_build_grid_fixes_policy_constants(self):
        for config in build_grid():
            self.assertIsNone(config.drift_onset_ticks)
            self.assertIsNone(config.drifted_client_ids)
            self.assertEqual(config.retrain_rounds, 1)
            self.assertEqual(config.production_horizon_seconds, 400.0)

    def test_result_directories_disambiguate_by_ramp_duration(self):
        from experiments.e07_drift_agent.run_matrix import _result_directory

        configs = build_grid()
        duplicates = Counter((c.corruption, c.severity, c.seed) for c in configs)
        directories = {
            _result_directory(Path("/tmp/e09-grid"), config, duplicates)
            for config in configs
        }
        self.assertEqual(len(directories), 60)
```

Atualizar `tests/test_experiment_registry.py`:

```python
    def test_registers_all_nine_experiments(self):
        self.assertEqual(
            [spec.experiment_id for spec in EXPERIMENTS],
            [
                "e01-static",
                "e02-ablation",
                "e03-timeout",
                "e04-static-comparison",
                "e05-temporal-drift",
                "e06-drift-agent-prototype",
                "e07-drift-agent",
                "e08-partial-drift",
                "e09-gradual-drift",
            ],
        )

    def test_e09_uses_the_new_results_namespace(self):
        self.assertEqual(
            output_path("e09-gradual-drift", "cifar-10"),
            Path("results/e09-gradual-drift/cifar-10"),
        )
        self.assertEqual(
            temporary_output_path("e09-gradual-drift", "cifar-10"),
            Path("output/e09-gradual-drift/cifar-10"),
        )
```

- [ ] **Step 2: Rodar e confirmar que falham**

Run: `.venv/bin/python -m unittest tests.test_experiment_registry -v`
Expected: FAIL (lista com 8 ids; import de `e09_drift_gradual` falha).

- [ ] **Step 3: Implementar**

Criar `experiments/e09_drift_gradual/__init__.py`:

```python
"""E09: gradual drift by data mixture fraction."""
```

Criar `experiments/e09_drift_gradual/run_grid.py`:

```python
"""Run the E09 grid: ramp duration x scenario x seed.

Scenario: global corruption on the calibrated severities, with the mixture
fraction rising linearly from 0 to 1 over a configured number of ticks.
Retraining keeps the E07 policy: quorum at the server, global retraining,
per-client current-distribution datasets.
"""

from __future__ import annotations

import argparse
import itertools
import sys
from pathlib import Path

_BASE = Path(__file__).resolve().parent.parent.parent
for _path in (str(_BASE), str(_BASE / "src")):
    if _path not in sys.path:
        sys.path.insert(0, _path)

from experiments.e07_drift_agent.episode import (
    DEFAULT_PRODUCTION_HORIZON_SECONDS,
    DriftEpisodeConfig,
)
from experiments.e07_drift_agent.run_matrix import run_matrix
from experiments.shared.registry import temporary_output_path

DEFAULT_SCENARIOS: tuple[tuple[str, int], ...] = (
    ("gaussian_noise", 3),
    ("frosted_glass_blur", 4),
    ("motion_blur", 1),
    ("fog", 4),
)


def build_grid(
    *,
    seeds: tuple[int, ...] = (42, 43, 44, 45, 46),
    scenarios: tuple[tuple[str, int], ...] = DEFAULT_SCENARIOS,
    ramp_ticks: tuple[int, ...] = (5, 10, 20),
) -> list[DriftEpisodeConfig]:
    """Materialize the scenario x seed x ramp-duration grid."""
    return [
        DriftEpisodeConfig(
            corruption=corruption,
            severity=severity,
            drift_ramp_ticks=ramp,
            seed=seed,
            retrain_rounds=1,
            production_horizon_seconds=DEFAULT_PRODUCTION_HORIZON_SECONDS,
        )
        for (corruption, severity), seed, ramp in itertools.product(
            scenarios, seeds, ramp_ticks
        )
    ]


def main(argv: list[str] | None = None) -> None:
    parser = argparse.ArgumentParser(description="Run the E09 gradual-drift grid")
    parser.add_argument("--seeds", nargs="+", type=int, default=[42, 43, 44, 45, 46])
    parser.add_argument("--ramp-ticks", nargs="+", type=int, default=[5, 10, 20])
    parser.add_argument(
        "--scenario",
        nargs="+",
        default=[f"{corruption}:{severity}" for corruption, severity in DEFAULT_SCENARIOS],
        help="one or more corruption:severity pairs",
    )
    parser.add_argument(
        "--output-dir",
        default=str(
            temporary_output_path("e09-gradual-drift", "cifar-10") / "grid-v1"
        ),
    )
    args = parser.parse_args(argv)

    from src.utils.corruptions import apply_corruption

    scenarios: list[tuple[str, int]] = []
    for value in args.scenario:
        corruption, separator, severity = value.rpartition(":")
        if not separator:
            raise SystemExit(
                f"scenario must use corruption:severity, got {value!r}"
            )
        scenarios.append((corruption, int(severity)))
    configs = build_grid(
        seeds=tuple(args.seeds),
        scenarios=tuple(scenarios),
        ramp_ticks=tuple(args.ramp_ticks),
    )
    run_matrix(configs, output_dir=args.output_dir, corruption_fn=apply_corruption)
    print(f"E09 grid done: {len(configs)} pairs -> {args.output_dir}")


if __name__ == "__main__":
    main()
```

Em `experiments/shared/registry.py`, após o bloco do E08:

```python
    ExperimentSpec(
        "e09-gradual-drift",
        "Drift gradual por fração de mistura",
        "current",
        Path("results/e09-gradual-drift"),
    ),
```

- [ ] **Step 4: Rodar e confirmar que passam**

Run: `.venv/bin/python -m unittest tests.test_e09_grid tests.test_experiment_registry tests.test_output_paths -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add experiments/e09_drift_gradual experiments/shared/registry.py tests/test_e09_grid.py tests/test_experiment_registry.py
git commit -m "feat(e09): register the gradual-drift family and grid runner"
```

---

### Task 6: Smoke end-to-end com rampa e catálogo

**Files:**
- Modify: `tests/test_drift_e2e_smoke.py`
- Modify: `docs/experimentos.md` (catálogo)
- Test: `tests/test_drift_e2e_smoke.py`

**Interfaces:**
- Consumes: `run_drift_comparison`, `run_matrix`, `plot_scenario` com o campo novo.
- Produces: evidência de smoke de que um par com rampa persiste, valida e plota.

- [ ] **Step 1: Escrever o teste que falha**

Adicionar a `tests/test_drift_e2e_smoke.py`:

```python
    def test_ramp_pair_persists_validates_and_plots(self):
        config = DriftEpisodeConfig(
            dataset="e2e-smoke",
            num_clients=1,
            initial_rounds=1,
            retrain_rounds=1,
            warmup_ticks=20,
            monitor_tick_seconds=1.0,
            monitor_ticks=1,
            batch_size=1,
            corruption="gaussian_noise",
            severity=1,
            drift_ramp_ticks=20,
            seed=17,
            production_horizon_seconds=1.0,
        )

        def runner(run_config, *, corruption_fn, **_kwargs):
            return run_drift_comparison(
                run_config,
                corruption_fn=corruption_fn,
                server_factory=_SmokeServer,
                monitor_factory=lambda _server, _baseline: _SmokeMonitor(),
            )

        with tempfile.TemporaryDirectory() as directory:
            root = Path(directory)
            paths = run_matrix(
                [config],
                output_dir=root,
                runner=runner,
                corruption_fn=_identity_corruption,
            )
            agent, baseline = load_persisted_pair(paths[0][0].parent)
            validate_pair(agent, baseline)
            self.assertEqual(
                agent.experiment_config["drift_ramp_ticks"], 20
            )
            image_path = plot_scenario(
                paths[0][0].parent.parent,
                root / "ramp-comparison.png",
            )
            self.assertGreater(image_path.stat().st_size, 0)
```

- [ ] **Step 2: Rodar e confirmar que falha**

Run: `.venv/bin/python -m unittest tests.test_drift_e2e_smoke -v`
Expected: FAIL (`KeyError: 'drift_ramp_ticks'` se a Task 4 não estiver aplicada; caso contrário o teste passa).

- [ ] **Step 3: Implementar**

Se a Task 4 já está aplicada, o teste passa sem código novo. Atualizar o
catálogo `docs/experimentos.md`: adicionar a linha E09 na tabela de famílias
e na linha do tempo, seguindo o formato das entradas E07/E08.

- [ ] **Step 4: Rodar a suíte completa e as verificações**

Run:

```bash
.venv/bin/python -m unittest discover -s tests 2>&1 | grep -E "^(Ran|OK|FAILED)"
.venv/bin/python -m compileall -q experiments tests
git diff --check
```

Expected: 5 falhas MPS pré-existentes e nada mais; compileall e diff limpos.

- [ ] **Step 5: Commit**

```bash
git add tests/test_drift_e2e_smoke.py docs/experimentos.md
git commit -m "test(e09): smoke the ramp pair through persistence and plot"
```

---

## Self-Review

**Cobertura da especificação (doc 18):**

- §3.1 fração por tempo virtual → Task 1 (`ramp_fraction`) e Task 2 (call site com `production_seconds`).
- §3.2 aplicação no batch → Task 2.
- §3.3 compatibilidade bit a bit → Task 2 (caminho `None` sem RNG extra, sem teste misto).
- §3.4 validação mútua com E08 → Task 1 (config) e Task 4 (schema v3).
- §4 teste misto → Task 3 (`_mixed_evaluation_test`, permutação `seed + 12_345`).
- §5 retreino por mistura → Task 3 (`build_mixed_dataset`, `client_permutations`).
- §6 parâmetros (20 ticks principal, {5,10,20} sensibilidade) → Task 5 (grid).
- §7 schema e métrica → Task 4 (schema) e Task 3 (`fraction_at_trigger`, `fraction_at_tau_crossing`).
- §8/9 alegações e limitações → documentadas no doc 18; métricas em Task 3.
- Registro da família → Task 5; catálogo → Task 6.

**Placeholder scan:** nenhum passo sem código.

**Type consistency:** `ramp_fraction(v, config)`, `corrupt_count(size, f)`,
`build_mixed_dataset(clean, corrupted, fraction, permutation)`,
`_mixed_evaluation_test(clean_test, corrupted_test, permutation, fraction)`
e `_current_evaluation_test(server, config, production_start, clean_test,
corrupted_test, permutation)` são definidos antes de cada uso.

---

## Emenda 2026-08-12 — horizonte de 600 s e desfecho de gatilho tardio

Decisões do usuário depois da primeira execução da matriz completa:

1. **Horizonte do grid E09: 600 s.** O `run_grid.py` usa
   `E09_PRODUCTION_HORIZON_SECONDS = 600.0`. A rampa de 200 s, uma
   detecção tardia, o retreino e a observação pós-retreino cabem na janela.
   O E07 permanece com 400 s. Teste do grid atualizado.
2. **Gatilho tardio vira desfecho válido.** O `RuntimeError` de orçamento
   foi substituído por: decisão registrada, `metrics["retrain_skipped_budget"]
   = True`, aviso no log, medição até o horizonte. A validação v3 aceita
   decisão sem rounds somente com o marcador. O resumo particiona com
   `retrain_skipped`. O plot marca com `x` e "(no retrain)".

Implementação: `episode.py`, `drift_results.py`, `run_matrix.py`,
`run_grid.py`, `plot.py` e testes. A tarefa 6 do plano original permanece
válida; estas mudanças estendem o contrato descrito na Task 1 (skip em vez
de erro no orçamento).

---

*Plano gerado em 2026-08-09 a partir do doc 18 e de
`checkpoints/e09-drift-gradual/`. Emendado em 2026-08-12.*
